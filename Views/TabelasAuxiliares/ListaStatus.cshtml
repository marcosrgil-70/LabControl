@model LabControl.Controllers.ListaAuxiliar
@{
    ViewData["Title"] = Model.Titulo;
    var actionSalvar = "Salvar" + Model.Entidade;
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h5 class="mb-0"><i class="bi bi-tags me-2 text-primary"></i>@Model.Titulo</h5>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left me-1"></i>Voltar
    </a>
</div>

<div class="row g-3">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header fw-semibold py-2">
                <i class="bi bi-plus-circle me-1"></i>Novo Registro
            </div>
            <div class="card-body">
                <form asp-action="@actionSalvar" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" id="editId" />
                    <div class="mb-3">
                        <label class="form-label">Descrição <span class="text-danger">*</span></label>
                        <input name="descricao" id="editDescricao" class="form-control form-control-sm" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Cor (para exibição)</label>
                        <div class="d-flex gap-2 align-items-center">
                            <input type="color" name="cor" id="editCor" class="form-control form-control-sm form-control-color" value="#6c757d" style="width: 60px;" />
                            <span class="text-muted small">Escolha uma cor para o badge de status</span>
                        </div>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary btn-sm flex-grow-1">
                            <i class="bi bi-save me-1"></i>Salvar
                        </button>
                        <button type="button" class="btn btn-outline-secondary btn-sm" onclick="limparForm()">Novo</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-md-8">
        <div class="card">
            <div class="card-body p-0">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>#</th>
                            <th>Descrição</th>
                            <th>Cor</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.Itens.Any())
                        {
                            <tr>
                                <td colspan="4" class="text-center text-muted py-3">Nenhum registro cadastrado.</td>
                            </tr>
                        }
                        @foreach (dynamic item in Model.Itens)
                        {
                            <tr>
                                <td class="text-muted">@item.Id</td>
                                <td>
                                    <span class="badge" style="background-color: @(item.Cor ?? "#6c757d")">
                                        @item.Descricao
                                    </span>
                                </td>
                                <td><code class="small">@(item.Cor ?? "-")</code></td>
                                <td class="text-end">
                                    <button class="btn btn-outline-secondary btn-sm"
                                            onclick="editar(@item.Id, '@item.Descricao', '@(item.Cor ?? "#6c757d")')">
                                        <i class="bi bi-pencil"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
    function editar(id, descricao, cor) {
        document.getElementById('editId').value = id;
        document.getElementById('editDescricao').value = descricao;
        document.getElementById('editCor').value = cor;
    }
    function limparForm() {
        document.getElementById('editId').value = '';
        document.getElementById('editDescricao').value = '';
        document.getElementById('editCor').value = '#6c757d';
    }
</script>
}
